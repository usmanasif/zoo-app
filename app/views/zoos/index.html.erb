<table id="zoos-table" class="table table-hover">
  <thead>
    <tr>
      <th> Image</th>
      <th> Name</th>
      <th> Address</th>
      <th> Address2</th>
      <th> City</th>
      <th> State</th>
      <th> Zip</th>
      <th> Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @zoos.each do |zoo| %>
      <tr>
        <td>
          <%= image_tag (zoo.image.url(:medium)) if zoo.image %>
        </td>
        <td><%=zoo.name%></td>
        <td><%=zoo.address%></td>
        <td><%=zoo.address2%></td>
        <td><%=zoo.city%></td>
        <td><%=zoo.state%></td>
        <td><%=zoo.zip%></td>
        <td><%= link_to("List Animals", zoo_animals_path(zoo.id))%></td>
      </tr>
    <% end %>
  </tbody>
</table>
